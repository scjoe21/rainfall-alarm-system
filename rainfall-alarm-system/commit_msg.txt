fix: API 한도 초과 방지 및 알람 연속성 개선

- 특보 체크 API(getWthrWrnList)를 일일 한도 카운팅에서 제외하여 강수량 API 소진 시에도 특보 감지 유지
- KMA_DAILY_LIMIT 기본값 10,000 -> 50,000으로 상향
- APIHUB AWS 캐시 히트 시 공공데이터포털 실황 API 호출 생략으로 일일 사용량 절감
- 알람 타이머 로직 변경: 새 알람 수신 시 기존 타이머 무시 -> 리셋(조건 지속 중 끊김 방지)
- 알람 표시 지속 시간 4분 50초 -> 5분 30초(폴링 5분 + 실행 여유)
- Fly.io 영구 볼륨 마운트 추가(/app/db): 재배포 시 DB 초기화 방지
- DB 경로를 DB_DIR 환경변수로 분리, 볼륨 디렉토리 자동 생성 처리
